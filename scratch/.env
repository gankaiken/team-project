# Leave empty to use Vite proxy (/api -> VITE_API_PROXY_TARGET)
VITE_API_BASE_URL=

# Local dev proxy target for /api calls
VITE_API_PROXY_TARGET=http://localhost:8787

# Enable only for demos when backend/model is unavailable
VITE_ENABLE_SCREENING_MOCK=false
